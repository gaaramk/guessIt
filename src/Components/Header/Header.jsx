const Header = () => {
  return (
    <header className="head">
      <div>
        <h1>Guess OR Die !</h1>
        <p>
          Guess the word within 8 attempts to keep the kidnapped hostages safe
          from Death !
        </p>
      </div>
    </header>
  );
};

export default Header;
